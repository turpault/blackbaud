# Blackbaud API Configuration
# All API credentials are now handled by the proxy server for better security

# Optional: Environment identifier
REACT_APP_ENVIRONMENT=development

# Note: OAuth2 credentials (Client ID, Client Secret) and Subscription Key 
# are now handled by the proxy server for better security. 
# The proxy server will provide both the access token and subscription key to this application. 